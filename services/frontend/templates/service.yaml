apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart.appServiceName" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "chart.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }} 
